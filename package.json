{
  "name": "topmoneytools",
  "version": "1.0.0",
  "description": "Top Money Tools Website",
  "scripts": {
    "build": "cp -r public/* .vercel/output || (mkdir -p .vercel/output && cp -r public/* .vercel/output)"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.19.0",
    "@google-cloud/vertexai": "^1.12.0",
    "@google/genai": "^2.8.0"
  }
}
